<!DOCTYPE html>
<html lang="en">   

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" type="text/css" href="/css/main.css">
    <title>Lego Collection</title>
</head>

    <body class="bg-gray-50">

        <%- include('./partials/navbar', {page: '/addSet' }) %>

        <div class="container mx-auto p-4">

            <div class="hero bg-gray-100 rounded-lg p-6 mb-8">
                <div class="hero-content text-center">
                    <div class="max-w-md">
                        <h1 class="text-5xl font-bold">Add Set</h1>
                        <p class="py-6">Use the form below to add a new Lego Set to the collection</p>
                    </div>
                </div>
            </div>
    
            <form action="/lego/addSet" method="post" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 gap-y-0 mt-8">
                <div class="mb-6">
                    <label for="name" class="block font-medium">Name</label>
                    <input type="text" id="name" name="name" required class="w-full border border-gray-300 rounded-md px-3 py-2 focus:border-blue-300 bg-white">
                </div>
                <div class="mb-6">
                    <label for="num_parts" class="block font-medium">Number of Parts</label>
                    <input type="number" id="num_parts" name="num_parts" required class="w-full border border-gray-300 rounded-md px-3 py-2 focus:border-blue-300 bg-white">
                </div>
                <div class="mb-6">
                    <label for="theme_id" class="block font-medium">Theme</label>
                    <select id="theme_id" name="theme_id" required class="w-full border border-gray-300 rounded-md px-3 py-2 focus:border-blue-300 bg-white">
                        <% themes.forEach(theme => { %>
                        <option value="<%= theme.id %>">
                            <%= theme.name %>
                        </option>
                        <% }) %>
                    </select>
                </div>
                <div class="mb-6">
                    <label for="year" class="block font-medium">Year</label>
                    <input type="number" id="year" name="year" required class="w-full border border-gray-300 rounded-md px-3 py-2 focus:border-blue-300 bg-white">
                </div>
                <div class="mb-6">
                    <label for="img_url" class="block font-medium">Image URL</label>
                    <input type="url" id="img_url" name="img_url" required class="w-full border border-gray-300 rounded-md px-3 py-2 focus:border-blue-300 bg-white">
                </div>
                <div class="mb-6">
                    <label for="set_num" class="block font-medium">Set Num</label>
                    <input type="text" id="set_num" name="set_num" required class="w-full border border-gray-300 rounded-md px-3 py-2 focus:border-blue-300 bg-white">
                </div>
                <div class="mt-6 col-span-full">
                    <button type="submit" class="btn btn-primary w-full">Add Set</button>
                </div>
            </form>
    
            <div class="bg-white py-8"></div>
            
        </div>
    </body>
    </html>