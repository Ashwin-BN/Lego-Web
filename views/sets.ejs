<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" type="text/css" href="/css/main.css">
    <title>Lego Collection</title>
    <style>
        .button-grey {
            color: white;
            background-color: gray;
        }
        
        .button-white {
            color: gray;
            background-color: white;
        }
    </style>
</head>

<body class="bg-gray-50">
    <%- include('./partials/navbar', {page: '/' }) %>

    <div class="container mx-auto p-4">

        <div class="hero bg-yellow-200 rounded-lg p-6">
            <div class="hero-content text-center">
                <div class="max-w-md">
                    <h1 class="text-5xl font-bold text-amber-900">Collection</h1>
                    <p class="py-6 text-lg text-yellow-950">Browse the entire collection. Click the theme to filter by themes such as:
                        <a href="/lego/sets?theme=Technic" class="btn btn-primary btn-xs">Technic</a>
                        <a href="/lego/sets?theme=Classic Town" class="btn btn-primary btn-xs">Classic Town</a>
                        <a href="/lego/sets?theme=Supplemental" class="btn btn-primary btn-xs">Supplemental</a>
                    </p>
                </div>
            </div>
        </div>
        <br>

        <div class="overflow-x-auto overflow-y-hidden">
            <table class="table table-zebra bg-white rounded-lg border-spacing-2">
                <tbody>
                    <% sets.forEach((set, index) => { %>
                    <tr>

                        <td>
                            <div class="avatar">
                                <div class="w-32 h-32 rounded-lg bg-transparent bg-no-repeat bg-cover"
                                    style="background-image: url('<%= set.img_url %>');"></div>
                            </div>
                        </td>

                        <td class="text-lg">
                            <%= set.name %> | <a href="/lego/sets?theme=<%= set.theme %>"
                                class="btn mt-2 btn-xs <%= index % 2 === 0 ? 'button-grey' : 'button-white' %> uppercase"><%= set.theme %></a>
                        </td>

                        <td class="text-lg">
                            <span class="font-bold">Year: </span> <%= set.year %> - <span class="font-bold">Parts:</span> <%= set.num_parts %>
                        </td>
                        <th>
                            <a href="/lego/sets/<%= set.set_num %>"
                                class="btn btn-xs <%= index % 2 === 0 ? 'button-grey' : 'button-white' %> uppercase">details</a>
                        </th>
                    </tr>
                    <% }) %>
                </tbody>
            </table>
        </div>

    </div>
</body>

</html>
